# P1.0A Cosmic Decisions

- Enforced centerXRatio and centerYRatio to 0.5 globally to ensure the central sun remains centered.
- Retained all nine celestial bodies as required.
- Verified that `pointer-events: none` is correctly applied to `cosmic-backdrop` and `CosmicOrbitEngine` to prevent orbit layers from overlapping controls.
